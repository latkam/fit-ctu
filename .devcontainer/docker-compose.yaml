services:
  devcontainer:
    build:
      context: .
      dockerfile: Dockerfile.local
    volumes:
      - ..:/workspace
      - ~/.git:/home/dockeruser/.git
      - ~/.ssh:/home/dockeruser/.ssh
    working_dir: /workspace
    tty: true
