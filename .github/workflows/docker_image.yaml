# name: Build Docker Image
# 
# on:
  # push:
    # paths:
      # - '.devcontainer/Dockerfile'
# 
# jobs:
  # build:
    # runs-on: ubuntu-latest
# 
    # steps:
      # Checkout the repository
      # - name: Checkout code
        # uses: actions/checkout@v3
# 
      # Build the Docker image
      # - name: Build Docker Image
        # run: |
          # docker build -t my-docker-image:latest -f .devcontainer/Dockerfile .